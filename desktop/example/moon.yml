$schema: '../../.config/moon/cache/schemas/project.json'

language: 'typescript'
layer: 'application'
stack: 'frontend'
tags: ['desktop-app']

project:
  title: 'Example · Design Kit'
  description: 'Showcase Tauri app for the GENSLATE Nord design system (titlebar · content · status bar).'

dependsOn:
  - 'design-system'
  - 'tauri-bridge'
  - 'tokens'
  - id: 'config-vite'
    scope: 'development'
  - id: 'config-typescript'
    scope: 'development'

tasks:
  test:
    args: ['--preload', './tests/setup/dom.preload.ts']
