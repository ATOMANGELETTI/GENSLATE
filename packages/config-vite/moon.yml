$schema: '../../.config/moon/cache/schemas/project.json'

language: 'typescript'
layer: 'configuration'
stack: 'frontend'
tags: ['config']

project:
  title: 'Config · Vite'
  description: 'Shared Vite 8 preset for Tauri apps (ports, TAURI_* env, React Compiler, Tailwind v4).'

dependsOn:
  - id: 'config-typescript'
    scope: 'development'

workspace:
  inheritedTasks:
    exclude: ['test']
