# rustfmt only discovers config in the crate/workspace root or its parents, so this file stays here.
edition = "2024"
style_edition = "2024"
max_width = 100
use_field_init_shorthand = true
use_try_shorthand = true
newline_style = "Unix"
