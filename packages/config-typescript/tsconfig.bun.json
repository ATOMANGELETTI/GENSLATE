{
  "$schema": "https://json.schemastore.org/tsconfig",
  "display": "GENSLATE — bun scripts & tooling",
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    "lib": ["ESNext"],
    "types": ["bun"]
  }
}
